{
  "name": "cobranca-ipad-backup",
  "private": true,
  "type": "module",
  "description": "Script standalone de backup automático — roda só no GitHub Actions, nunca faz parte do build/deploy da aplicação (Vercel). Dependências isoladas de propósito.",
  "scripts": {
    "backup": "node backup.mjs",
    "restore": "node restore.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@supabase/supabase-js": "^2.112.3"
  }
}
